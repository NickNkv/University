#include <iostream>
#include <fstream>
#include "Person.hpp";

int main() {

	return 0;
}

bool write(std::ostream& out, const Person& p) {
	if (!out) {
		return false;
	}

	size_t len = strlen(p.);
}